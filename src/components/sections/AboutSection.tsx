type AboutProps = {
  isHome?: boolean;
};

const About: React.FC<AboutProps> = ({ isHome = false }) => {
  return (
    <div className="flex flex-col items-center section-outer space-y-20">
      
    </div>
  );
};

export default About;
